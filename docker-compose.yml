version: "3.8"
services:
  web:
    build: .
    ports:
      - "5000:5000"
      - "6500:6500"
    volumes:
      - ./uploads:/app/uploads
      - ./results:/app/results
    environment:
      - FLASK_ENV=production
    restart: always
